# Complete Nginx Docker reverse proxy config file
server {
  listen 80;
  listen [::]:80;
  server_name grupo2.ettore.page;

  location / {
    root /usr/share/nginx/html;
    index index.html index.htm;
  }

} # End of Docker Nginx reverse proxy example file